{
  "name": "@appliedblockchain/chainlink-contracts",
  "version": "0.0.2",
  "description": "",
  "scripts": {
    "test": "hardhat test",
    "lint": "eslint . --ext .js --ext .ts ",
    "lint:fix": "eslint . --ext .js --ext .ts  --fix",
    "compile": "hardhat compile",
    "prepare": "npm run compile && rm -rf artifacts && rm -rf cache",
    "build": "tsc"
  },
  "license": "MIT",
  "devDependencies": {
    "@appliedblockchain/chainlink-eslint-config": "0.0.1",
    "@typechain/ethers-v5": "^7.0.1",
    "@typechain/hardhat": "^2.2.0",
    "@types/mocha": "^9.0.0",
    "eslint": "^7.32.0",
    "hardhat": "^2.0.6",
    "hardhat-abi-exporter": "^2.2.1",
    "hardhat-deploy": "^0.7.0-beta.39",
    "mocha": "^9.0.3",
    "ts-node": "^10.1.0",
    "tsc": "^2.0.3",
    "typechain": "^5.1.2",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.1.9",
    "@chainlink/test-helpers": "0.0.5",
    "@chainlink/token": "^1.1.0"
  }
}
